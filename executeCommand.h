#ifndef EXECUTE_COMMAND_H
#define EXECUTE_COMMAND_H

#include "systemState.h"
#include "commands.h"

// Declares the executeCommand function
void executeCommand(Command *command, SystemState *sysState);

#endif // EXECUTE_COMMAND_H